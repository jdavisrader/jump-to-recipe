{
  "name": "jump-to-recipe-workspace",
  "version": "1.0.0",
  "description": "Jump to Recipe - Recipe and cookbook management platform",
  "private": true,
  "scripts": {
    "dev": "cd jump-to-recipe && npm run dev",
    "build": "cd jump-to-recipe && npm run build",
    "start": "cd jump-to-recipe && npm run start",
    "lint": "cd jump-to-recipe && npm run lint",
    "test": "cd jump-to-recipe && npm test"
  },
  "workspaces": [
    "jump-to-recipe"
  ],
  "dependencies": {
    "@hello-pangea/dnd": "^18.0.1",
    "@radix-ui/react-switch": "^1.2.5"
  }
}
